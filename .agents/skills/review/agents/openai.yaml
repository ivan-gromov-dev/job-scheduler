interface:
  display_name: "Review"
  short_description: "Review changes for correctness and regressions"
  default_prompt: "Use $review to review the current changes and report actionable findings."
